<%= @counts[0] %> Total Stores within 25 miles
Displayng <%= @counts[-1] %> Stores

<% @stores.each do |store| %>
  Name: <%=link_to store.name, store_path(store, name: store.name, address: store.address, ) %>
  City: <%= store.city %>
  Distance: <%= store.distance %>
  Phone: <%= store.phone %>
  Store Type: <%= store.store_type %>
<% end %>
