<%= @counts[0] %> Total Stores within 25 miles
Displayng <%= @counts[-1] %> Stores

<% @stores.each do |store| %>
  Name: <%=link_to store.name, store_path(store.sid, name: store.name, address: store.address, city: store.city, state: store.state, zip: store.zip, store_type: store.store_type) %>
  City: <%= store.city %>
  Distance: <%= store.distance %>
  Phone: <%= store.phone %>
  Store Type: <%= store.store_type %>
<% end %>
